apiVersion: v1
kind: Service
metadata:
  name: ecommerce-service-cv
  labels:
    app: ecommerce
spec:
  selector:
    app: ecommerce
  ports:
    - port: 5005
      targetPort: 5005
      protocol: TCP
      name: ecommerce-port
